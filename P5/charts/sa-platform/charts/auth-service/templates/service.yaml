apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-auth-service
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "sa-platform.labels" (dict "root" . "component" "auth-service") | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "sa-platform.selectorLabels" (dict "component" "auth-service") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port | default 4001 }}
      targetPort: http
