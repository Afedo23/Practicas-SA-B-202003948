apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-auth-service-config
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "sa-platform.labels" (dict "root" . "component" "auth-service") | nindent 4 }}
data:
  DB_NAME: "authdb"
  PORT: "4001"
