apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-notificaciones-service-config
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "sa-platform.labels" (dict "root" . "component" "notificaciones-service") | nindent 4 }}
data:
  DB_NAME: "auditoriadb"
